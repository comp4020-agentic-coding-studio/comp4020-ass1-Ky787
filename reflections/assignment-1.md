# Assignment 1

> **Draft — mine to write.** 150–300 words, in my own voice. The notes under
> each prompt are the factual record to draw on, not the answer; they come out
> when I write the entry.

## What was the breakthrough that moved the work forward?

_[Write this.]_

Candidates from the record, if one of them is the honest answer:

- Putting the dataset-integrity rule in `CLAUDE.md` before any code existed, and
  watching it decide implementation questions later — the assembly renderer
  colouring Capstone's own text rather than re-printing it, the inspector
  admitting the dataset records no LLVM-to-machine block mapping.
- Realising a green check could be green for the wrong reason
  (`overflow-x: hidden` clamps `scrollWidth`), and that removing the declaration
  mattered more than adding another test.
- Verifying the Hikari flags myself instead of accepting the agent's hedge, and
  finding `-mllvm -bcf_prob=100` that it did not know about.
- Seeing that a correct graph can still be unreadable, and that fixing it was a
  layout problem, not a data problem.

## What did this work change about who I want to be as a software developer?

_[Write this.]_

Things in the record that might bear on it, if they are true for me:

- The agent hedged when it could not verify something, and the hedge was only
  resolved by me going and checking. What that says about where the human
  belongs in the loop.
- The parts of this build that held up were the ones with a sensor behind them —
  256 files re-checked against the index, 82,949 instructions re-derived, two
  viewports driven in a real browser. The parts I had to redo were the ones I
  had only looked at.
- A rule written into the harness kept paying out; a rule repeated in a prompt
  did not.
